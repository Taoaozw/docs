---
title: 'List Variants'
openapi: 'GET /api/openapi/products/{spuCode}/variants'
---

Returns all variants (SKUs) for a specific product. Use the `skuCode` from the response in the `sku` field when creating orders.
